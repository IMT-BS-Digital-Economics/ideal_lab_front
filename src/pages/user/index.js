import NavBar from "../../components/layout/navBar";
import UserPage from "../../components/user";

const user = () => {
    return (
        <div>
            <NavBar/>
            <UserPage/>
        </div>
    );
}

export default user;