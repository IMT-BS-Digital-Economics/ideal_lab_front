import { Rubik } from 'next/font/google';

// Use Google Fonts as an example source
const rubik = Rubik({
    googleFont: {
        weights: ['400', '500', '700'],
    },
    subsets: ['latin'],
});

export default rubik;
